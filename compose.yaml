services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      platforms:
        - 'linux/amd64'
    container_name: openfisca-app
    ports:
      - "18080:8080"
    volumes:
      - "./openfisca_nsw_safeguard/:/app/openfisca_nsw_safeguard/"
      - "./Makefile:/app/Makefile"
    restart: unless-stopped
